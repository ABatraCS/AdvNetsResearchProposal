# AdvNetsResearchProposal

## Requirements

- `make`
- A TeX Live (or equivalent) install providing `latexmk`, `pdflatex`, and `bibtex`

## Build

```
make
```

Output PDF is written to `generated/`.

```
make clean
```

Removes the `generated/` directory.
